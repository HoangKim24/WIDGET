name: LichTuanApp

options:
  bundleIdPrefix: com.hoangkim24
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: 5.9

targets:
  LichTuanApp:
    type: application
    platform: iOS
    deploymentTarget: 16.0
    sources:
      - path: LichTuanApp
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hoangkim24.lichtuan
        INFOPLIST_FILE: LichTuanApp/Info.plist
    dependencies:
      - target: LichTuanWidget

  LichTuanWidget:
    type: app-extension
    platform: iOS
    deploymentTarget: 16.0
    sources:
      - path: LichTuanWidget
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hoangkim24.lichtuan.widget
        INFOPLIST_FILE: LichTuanWidget/Info.plist
        APPLICATION_EXTENSION_API_ONLY: YES

  LichTuanAppTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: 16.0
    sources:
      - path: LichTuanAppTests
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hoangkim24.lichtuan.tests
        GENERATE_INFOPLIST_FILE: YES

  LichTuanAppUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: 16.0
    sources:
      - path: LichTuanAppUITests
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hoangkim24.lichtuan.uitests
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: LichTuanApp

schemes:
  LichTuanApp:
    build:
      targets:
        LichTuanApp: all
        LichTuanWidget: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - LichTuanAppTests
        - LichTuanAppUITests
